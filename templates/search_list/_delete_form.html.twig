<form method="post" action="{{ path('search_list_delete', {'id': search_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ search_list.id) }}">
    <button class="btn">Delete</button>
</form>
